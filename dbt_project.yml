name: 'my_new_project'
version: '1.0.0'
config-version: 2

# Define model configurations
models:
  my_new_project:
    staging:
      materialized: view
    intermediate:
      materialized: view
    marts:
      +materialized: table
