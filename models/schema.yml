version: 2

models:
    - name: customers
      description: "A table containing customer details"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null